part of 'children_bloc.dart';

// ChildrenListState - represents a state with a list of children
@immutable
class ChildrenListState {
  final List<Child> children;

  const ChildrenListState(this.children);
}
